spring.datasource.url=jdbc:mariadb://mariadb:3306/${MARIADB_DATABASE}
spring.datasource.username=${MARIADB_USERNAME}
spring.datasource.password=${MARIADB_PASSWORD}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.database=MYSQL
spring.liquibase.url=jdbc:mariadb://mariadb:3306/${MARIADB_DATABASE}
spring.liquibase.user=${MARIADB_USERNAME}
spring.liquibase.password=${MARIADB_PASSWORD}
spring.liquibase.change-log=db/changelog/db.changelog-master.xml
